export class Calculator {
  constructor(age) {
    this.age = age;
  }

  calculateEarth() {
    let earthAge = Math.floor(this.age);
    return earthAge;
  }

  calculateMercury() {
    return mercuryAge;
  }
}